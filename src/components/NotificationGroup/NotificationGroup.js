import React from 'react';

import './NotificationGroup.css';

const NotificationGroup = ({ children }) => {
  return <div className="group-container">{children}</div>;
};

export default NotificationGroup;
